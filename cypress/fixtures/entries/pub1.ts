import * as Types from "@wpazderski/kvapi-types";
import { Entry } from "./Entry";

export const pub1: Entry = {
    key: "pub1k" as Types.data.entry.Key,
    value: "pub1v" as Types.data.entry.Value,
};
